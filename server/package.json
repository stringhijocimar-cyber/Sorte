{
  "name": "speakflow-ai-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Proxy de conversação do SpeakFlow AI: mantém a credencial do provedor fora do aplicativo.",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "start": "node index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "~6.0.3"
  }
}
